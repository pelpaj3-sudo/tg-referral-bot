# Реферальний Telegram-бот (Mini App)

Бот нараховує користувачам бонус за кожного друга, який приєднався за реферальним
посиланням і підписався на канал. Уся взаємодія відбувається через Telegram Mini
App — яскравий застосунок, що відкривається кнопкою «Кабінет». Чат-бот сам по собі
не має текстового меню — лише `/start` з кнопкою відкриття кабінету та сповіщення
адмінам про нові заявки на вивід.

## Можливості

- Реферальне посилання виду `https://t.me/BotName?startapp=<id>` — відкриває Mini App одразу
- Нарахування бонусу лише після підписки на канал (верифікація через `getChatMember`)
- У кабінеті: баланс, прогрес до виводу, реферальне посилання (копіювати/поділитися),
  список рефералів зі статусами, форма виведення коштів
- Адмін-вкладка в кабінеті: статистика, топ-реферери, заявки на виведення
  (Виплачено/Відхилити), розсилка всім користувачам
- Заявки на виведення додатково приходять адміну в чат з кнопками підтвердження —
  на випадок, якщо кабінет не відкритий
- Дані зберігаються у файлі `data/db.json` (без зовнішньої БД)

## Налаштування

1. Встановіть залежності:
   ```bash
   npm install
   ```
2. Скопіюйте `.env.example` у `.env` і заповніть:
   - `BOT_TOKEN` — токен від [@BotFather](https://t.me/BotFather)
   - `ADMIN_IDS` — ваш Telegram ID (і колег через кому). Дізнатись ID можна у [@userinfobot](https://t.me/userinfobot)
   - `CHANNEL_USERNAME` — публічний канал, напр. `@my_channel`. Для приватного каналу
     залиште це поле порожнім і заповніть `CHANNEL_ID` (chat id виду `-100...`) та
     `CHANNEL_INVITE_LINK`
   - `REFERRAL_BONUS` — сума за реферала (за замовчуванням 7)
   - `MIN_WITHDRAWAL` — мінімальна сума виведення (за замовчуванням 70)
   - `MINI_APP_URL` — публічна HTTPS-адреса, на якій задеплоєно застосунок
3. **Важливо:** додайте бота адміністратором каналу — інакше перевірка підписки
   (`getChatMember`) не працюватиме.
4. Поки канал ще не створено, можна лишити `CHANNEL_USERNAME`/`CHANNEL_ID` порожніми —
   кабінет тимчасово вважатиме всіх користувачів верифікованими.
5. Запуск:
   ```bash
   npm start
   ```

## Як це працює для користувача

1. Переходить за реферальним посиланням (`?startapp=<id_запросившого>`) — Telegram
   одразу відкриває Mini App
2. Якщо ще не підписаний на канал — кабінет показує екран з проханням підписатись
   і кнопкою «Я підписався»
3. Після підтвердження підписки тому, хто запросив, нараховується бонус
4. У кабінеті: баланс, своє посилання, список рефералів, виведення коштів
5. При виведенні кабінет одразу показує заявку адміну (в кабінеті й чатом) з
   кнопками підтвердження

## Адмін-вкладка

Видна в кабінеті лише користувачам з ID у `ADMIN_IDS`:

- **Статистика** — кількість користувачів, верифікованих, зарахованих рефералів,
  нараховано/виплачено, заявки в очікуванні, топ-10 реферерів
- **Заявки на виведення** — кнопки «Виплачено» / «Відхилити» прямо в кабінеті
  (при відхиленні кошти повертаються користувачу на баланс)
- **Розсилка** — текст надсилається всім користувачам бота

## Mini App

Технічно це Express-сервер (`src/server.js`), що піднімається в **тому самому
процесі**, що й бот (`src/index.js` запускає обидва), і роздає статичні файли
з `public/`. Авторизація запитів іде через підписані Telegram `initData`
(`src/telegramAuth.js`) — окремого логіна не потрібно.

**Обов'язкова умова:** Telegram відкриває Mini App лише за HTTPS-адресою.
Локальний `http://localhost` не підійде — потрібен публічний хостинг
(див. розділ «Розгортання» нижче) або тимчасовий тунель (`localtunnel`, `ngrok`)
для розробки.

**Рекомендовано:** зареєструйте Mini App через [@BotFather](https://t.me/BotFather) —
`/mybots` → оберіть бота → **Bot Settings** → **Configure Mini App** → **New App**
(вкажіть той самий `MINI_APP_URL`, назву, опис, іконку). Це вмикає велику кнопку
«Відкрити застосунок» на сторінці профілю бота і робить `?startapp=` посилання
надійнішими на всіх клієнтах Telegram. Chat Menu Button (кнопка «Кабінет» біля
поля вводу) бот встановлює сам через Bot API — цей крок BotFather для неї не
обов'язковий, лише для кнопки на профілі.

## Розгортання (Render.com, безкоштовний план)

1. Заведіть акаунт на [render.com](https://render.com) (можна через GitHub).
2. Залийте цей репозиторій у свій GitHub (новий приватний або публічний репозиторій).
3. У Render: **New → Web Service** → підключіть репозиторій.
   - Build command: `npm install`
   - Start command: `npm start`
   - План: **Free**
4. У розділі Environment додайте змінні (значення — з вашого `.env`):
   `BOT_TOKEN`, `ADMIN_IDS`, `CHANNEL_USERNAME` (або `CHANNEL_ID`+`CHANNEL_INVITE_LINK`),
   `REFERRAL_BONUS`, `MIN_WITHDRAWAL`, `SUPPORT_CONTACT`.
5. `MINI_APP_URL` — адреса самого сервісу: `https://<назва-сервісу>.onrender.com`
   (назву ви задаєте самі при створенні сервісу, адреса передбачувана заздалегідь).
   `PORT` можна не чіпати — Render підставляє його автоматично.
6. Deploy. Після першого успішного запуску кнопка Mini App у боті запрацює сама
   (бот викликає `setChatMenuButton` при старті).

**Важливо про дані на безкоштовному плані:** `data/db.json` зберігається на диску
контейнера, а безкоштовні інстанси Render не мають постійного диска — при
редеплої файл може обнулитися. Сервер сам пінгує себе кожні 10 хв, щоб інстанс
не засинав через бездіяльність (`src/index.js`). Для реального запуску з живими
користувачами варто підключити Render Disk (платно) або перейти на
SQLite/PostgreSQL.

## Структура проєкту

```
src/
  config.js       — читання .env
  db.js           — зберігання даних (users, withdrawals) у data/db.json
  texts.js        — тексти сповіщень (start, реферал зарахований, вивід)
  keyboards.js    — клавіатура заявки на вивід (Виплачено/Відхилити)
  telegram.js     — спільний екземпляр Telegraf-бота
  services.js     — спільна бізнес-логіка (статистика, верифікація+нарахування)
  telegramAuth.js — перевірка підпису Telegram Mini App initData
  bot.js          — /start, кнопка кабінету, обробка заявок на вивід із чату
  server.js       — Express API для Mini App
  index.js        — точка входу: запускає і бота, і сервер, і self-ping
public/
  index.html, style.css, app.js, telegram-web-app.js — фронтенд Mini App
data/
  db.json       — створюється автоматично при першому запуску
```

## Нотатки з дизайну рішення

- Уся функціональність живе в Mini App; бот у чаті — лише точка входу й дублюючі
  сповіщення адміну про заявки на вивід (щоб не пропустити, навіть не відкриваючи кабінет).
- Виведення завжди на всю суму балансу (без ручного вводу суми) — простіше й менше
  кроків для користувача.
- Заявка на виведення одразу списує суму з балансу; якщо адмін відхилить — кошти
  повертаються.
- Дані зберігаються у звичайному JSON-файлі — для обсягів у сотні/тисячі користувачів
  цього достатньо. За потреби масштабування можна перейти на SQLite/PostgreSQL,
  зберігши той самий інтерфейс `db.js`.
